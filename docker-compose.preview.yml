version: '3.8'

services:
  lyfehub:
    build: .
    container_name: lyfehub-orgs-preview
    ports:
      - "4003:3000"
    volumes:
      - /root/lyfehub-orgs-preview-data:/data
    environment:
      - NODE_ENV=production
      - JWT_SECRET=preview-secret-key-12345
    restart: unless-stopped
