services:
  kanban-preview:
    build: .
    container_name: kanban-preview-bases
    ports:
      - '3001:3000'
    volumes:
      - /root/kanban-preview-data:/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
